// ignore_for_file: constant_identifier_names

enum CardBrand {
  unknown,
  mastercard,
  visa,
  citigroup,
  discover,
  jcb,
  unionPay,
  bankOfAmerica,
  americaExpress,
  chase,
  wellsFargo
}

enum DepositsRadioFormState { normal, active, activeDisabled, disabled }

enum DepositsCheckboxFormState { normal, active, activeDisabled, disabled }

enum DepositsIndeterminantFormState { normal, active, activeDisabled, disabled }

enum DepositsToastStates { normal, info, warning, error, success }

enum DepositsFontFamilies {
  CircularStd,
  Roboto,
  SFProText,
  SFProRounded,
  SFProDisplay,
  HeroNew
}

enum DepositsTabsAndroidStates {
  text,
  iconTextVertical,
  icon,
  textFull,
  iconTextFull
}

enum DepositsFormStates { normal, active, cursor, error, disabled }

enum DepositsFormUploadStates {
  uploading,
  uploaded,
  active,
  disabled,
  error,
  normal
}

enum DepositsPasswordStrengthStates { normal, weak, fair, good, strong }

enum DepositsFormSizeStates { small, medium, large }

enum DepositsBoolStates { isTrue, isFalse }

enum DepositsSwitchFormState { on, off }

enum DepositsHrSizes { small, medium, large }

enum DepositsAvatarSizes { small, medium, large }

enum DepositsButtonSize { massive, huge, xLarge, large, medium, small }

enum DepositsButtonStates {
  normal,
  focus,
  hover,
  disabled,
  hasIcon,
  hasDropdown
}

enum DepositsButtonTypes {
  primary,
  disabled,
  invincible,
  normal,
  outline,
  error,
  success
}

enum DepositsBankLogos {
  bankBankOfAmerica,
  bankCiti,
  bankOO,
  bankTemplateOne,
  bankTemplateTwo,
  bankTemplateThree,
  bankUS,
  bankWellsFargo
}

enum DepositsCardLogos {
  cardAmericaExpress,
  cardDiscover,
  cardJcb,
  cardMaster,
  cardVisa
}
