import 'package:flutter/material.dart';

class DepositsCardSummaryFullModel {
  String title;
  Color titleColor;
  String subtitle;
  Color subtitleColor;

  DepositsCardSummaryFullModel({
    required this.title,
    required this.titleColor,
    required this.subtitle,
    required this.subtitleColor,
  });
}
