import 'package:flutter/material.dart';

class DepositsChecklistModel {
  String title;
  Color titleColor;
  IconData icon;
  Color iconColor;
  Color iconBgColor;

  DepositsChecklistModel({
    required this.title,
    required this.titleColor,
    required this.icon,
    required this.iconColor,
    required this.iconBgColor,
  });
}
